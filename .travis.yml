sudo: required

services:
  - docker

env:
  LANGUAGE=cwl
  LANGUAGE=wdl

notifications:
  slack: 
    on_success: never
    on_failure: always
    rooms:
      - oicr:S9k4EowgQv9AnbCfEZHSzCsg

language: java
jdk: 
  - oraclejdk8

before_install:
  - before-install.sh

install: 
  - install.sh

script: 
  - script.sh